[project]
name = "sankey"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "corson94", email = "jamie.corson.20@ucl.ac.uk" }
]
requires-python = ">=3.13"
dependencies = [
    "kaleido>=1.3.0",
    "pandas>=3.0.5",
    "plotly>=6.9.0",
]

[project.scripts]
sankey = "sankey_diagram:make_figure"

[build-system]
requires = ["uv_build>=0.11.26,<0.12.0"]
build-backend = "uv_build"
